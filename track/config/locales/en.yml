# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  activerecord:
    attributes:
      ledger:
        date: "date"
        name: "name"
        face_value: "face value"
        proportion_rate: "proportion rate"
        proportion_amount: "proportion amount"
      dollar_yen_transaction:
        date: "date"
        deposit_quantity: "Received USD"
        deposit_rate: "USD/JPY"
        withdrawal_quantity: "Sold USD"
        exchange_en: "Received Japanese Yen"
    activemodel:
      attributes:
        requests/dollar_yens_transaction:
          date: "date"
          deposit_quantity: "Deposit Amount (USD)"
          deposit_rate: "Deposit rate"
    errors:
      models:
        setting:
          attributes:
            language:
              inclusion: "must be either ja or en"
      messages:
        blank: "can't be blank"
        too_many_decimal_places: "must have at most 2 decimal places"
        must_be_integer: "must be an integer"
        too_long: "is too long (maximum is %{count} characters)"
        not_a_number: "is not a number"
        not_an_integer: "must be an integer"
        must_be_greater_than_or_equal_to_zero: "must be greater than or equal to 0"
  header:
    logout: "Logout"
    nav:
      dollar_yen_transactions:
        exchange: "FX Gain/Loss"
        ledger: "FCD GL (USD/JPY)"
        type: "Trade Type"
  footer:
    nav:
      help: "Help"
  views:
    dollar_yen_transactions:
      index: "FCD GL (USD/JPY)"
    ledgers:
      index: "Journal Entry"
    import_files:
      index: "csv Import Results Page"
    tax_returns:
      items:
        dc: "DC"
        first_estimated_income_tax_prepayment: "first_estimated_income_tax_prepayment"
        second_estimated_income_tax_prepayment: "second_estimated_income_tax_prepayment"
        miscellaneous_income: "miscellaneous_income"
        sales: 'sales'
    settings:
      index: "Settings"
      accounts:
        index: "Account"
      default_year:
        index: "default year"
      account:
        show: "account info"
        remove: "delete my account"