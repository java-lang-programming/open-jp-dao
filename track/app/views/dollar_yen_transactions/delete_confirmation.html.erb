<% content_for :title do %>WanWan ドル円外貨預金元帳 削除確認<% end %>

<% content_for :header do %>
  <%= render "layouts/header", user: @user, notification: @notification %>
<% end %>

<div class="bg-white mx-auto max-w-screen-xl py-4">
  <div class="mx-auto flex px-4">
    <div class="mx-auto"><%= @message %></div>
  </div>
  <div class="mx-auto flex justify-center px-4 py-4 space-x-4">
    <%= link_to "キャンセル", dollar_yen_transactions_path, class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
    <%= link_to "実行する", dollar_yen_transaction_path(@dollar_yen_transaction), data: { turbo_method: :delete }, class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded' %>
  </div>
</div>
