[tool.poetry]
name = "agent"
version = "0.1.0"
description = "AI Agent tools for Rails Dev Container"
authors = ["Masa"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.14"
ollama = "^0.6.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
